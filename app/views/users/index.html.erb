<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="digg_pagination">  <!-- apple_pagination / flickr_pagination -->
	<%= will_paginate %>
</div>

<ul class="users">
	<!-- <% @users.each do |user| %> -->
		<!-- @users.each is of User class, so Rails find _user.html.erb -->
		<!-- user - is not partial, it's variable of User class -->
		<!-- <%= render user %> -->
	<!-- <% end %> -->

	<!-- or the same (best solution) -->
	<%= render @users %>
</ul>

<%= will_paginate %>